import { OrderedMap } from 'immutable';

export function calcDiffEditorState(editorState, networkBlockMap) {
  const diff = {};
  editorState.getCurrentContent().getBlockMap().keySeq().toArray().forEach(key => {

  });
}
